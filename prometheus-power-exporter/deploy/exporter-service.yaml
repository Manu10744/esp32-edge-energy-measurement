apiVersion: v1
kind: Service
metadata:
  namespace: monitoring
  name: prometheus-power-exporter-service
  labels:
    app: prometheus-power-exporter
  annotations:
    prometheus.io/scrape: 'true'
spec:
  selector:
    app: prometheus-power-exporter
  type: NodePort
  ports:
  - name: http
    port: 8080
    targetPort: 8000