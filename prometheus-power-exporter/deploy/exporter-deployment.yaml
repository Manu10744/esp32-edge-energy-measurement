apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: monitoring
  name: prometheus-power-exporter-deployment
  labels:
    app: prometheus-power-exporter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus-power-exporter
  template:
    metadata:
      labels:
        app: prometheus-power-exporter
    spec:
      containers:
      - name: prometheus-power-exporter
        image: phyz1x/power-prometheus-exporter:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000